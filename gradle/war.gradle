apply plugin: "war"

bootWar {
    mainClassName = "com.demo.test.JhipsterMicroServicesApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
